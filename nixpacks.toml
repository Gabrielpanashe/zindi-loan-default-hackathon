[phases.install]
cmds = [
  "pip install -r requirements.txt -r risk_platform/requirements-api.txt"
]

[phases.build]
cmds = []

[start]
cmd = "cd risk_platform/backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
