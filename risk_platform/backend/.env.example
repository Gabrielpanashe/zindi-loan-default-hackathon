# Copy this to .env and adjust as needed

# Development without Redis — runs Celery tasks synchronously in-process
CELERY_TASK_ALWAYS_EAGER=true

# Production: set a real secret and switch to PostgreSQL
# JWT_SECRET=change-me-in-production
# DATABASE_URL=postgresql://user:pass@localhost:5432/loanrisk
